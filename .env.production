# Vite 构建时自动设为 production，此处勿写 NODE_ENV
VITE_APP_MODE=production

# 生产：相对路径 /api = 与部署站点同源（任意域名/端口均可，非写死某个例子）
# Nginx 将 /api 转发到 Spring Boot，见 docs/JZTK_NGINX.md
VITE_JZTK_API_URL=/api
# 可选：同步落库，需 Nginx 同样将 /api/jztk/sync 转发到后端
# VITE_JZTK_SYNC_URL=/api/jztk/sync
