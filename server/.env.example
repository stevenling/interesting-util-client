# 复制为 server/.env 后填写。本文件只放占位说明，禁止写入真实密码/密钥。
# server/.env 已在 .gitignore，切勿把含真实值的文件提交到 Git。

# 或使用一条 URL（设置了则忽略下面的 MYSQL_*）
# DATABASE_URL=mysql+pymysql://YOUR_USER:YOUR_PASSWORD@127.0.0.1:3306/your_database?charset=utf8mb4

# 开发可留空，应用内有开发用默认值；生产必须改为强随机串（勿使用示例值）
# AUTH_JWT_SECRET=

# 可选：注册验证码发信（与 Node 版 SMTP_* 一致）
# SMTP_HOST=
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASS=
# SMTP_FROM=
# SMTP_SKIP_SEND=1

# CORS，逗号分隔
# CORS_ORIGINS=http://localhost:8080,http://127.0.0.1:8080
